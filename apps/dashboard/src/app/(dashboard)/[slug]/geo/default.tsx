/**
 * Parallel-route children slot. Rendering the GEO overview here makes every
 * sibling navigation (Content Gaps, Prompts, …) flash that overview skeleton
 * until the matched page arrives.
 */
export default function Default() {
  return null;
}
